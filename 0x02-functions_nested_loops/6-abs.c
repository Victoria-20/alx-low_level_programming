#include "main.h"
#include <stdlib.h>
/**
* _abs - Print absolute value of a number
* @n: interger type  number
* Return: 0 Success
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
