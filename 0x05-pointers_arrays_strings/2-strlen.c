#include "main.h"
#include <string.h>

/**
 * _strlen - updates the value of variable n using pointers
 * @s: int variable to be swapped
 * Return: Always 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
